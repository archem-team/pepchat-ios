appId: chat.zeko.app
---
# Smoke Test — read-only navigation test (safe for production)
# Verifies login, channel navigation, and settings without mutating any data.

- runFlow:
    file: flows/login.yaml
    env:
      EMAIL: topasture@gmail.com
      PASSWORD: temp4now

# Navigate channels (read-only)
- runFlow:
    file: flows/navigate_channels.yaml

# Logout (navigates to You → Settings → Log Out)
- runFlow:
    file: flows/logout.yaml
